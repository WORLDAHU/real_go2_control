# 早期整机实验

本目录保存项目早期的整机模型加载、足端力到关节力矩、站立、跳跃时间线、MuJoCo 转换、
仿真显示和数据记录实验。

当前项目重点已经转向 GO2 左后腿单腿导轨和真实三电机验证，因此这些脚本不再放在日常入口目录。

归档这些运行脚本不代表废弃 `src/real_go2_model.py`。其中基于 Pinocchio 的正运动学、足端位置和
Jacobian 仍可用于后续运动学对照和整机研究。

本目录脚本的相对路径假设可能因归档而失效，运行前必须先检查 `PROJECT_ROOT` 和模型路径。
